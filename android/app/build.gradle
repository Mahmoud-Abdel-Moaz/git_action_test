plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
    id("com.google.gms.google-services")
}

def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')
if (flutterVersionCode == null) {
    flutterVersionCode = '1'
}

def flutterVersionName = localProperties.getProperty('flutter.versionName')
if (flutterVersionName == null) {
    flutterVersionName = '1.0'
}

def keystoreProperties = new Properties()
def keystoreProperties1 = new Properties()
def keystoreProperties2 = new Properties()
def keystoreProperties3 = new Properties()

def keystorePropertiesFileBlink = rootProject.file('key.properties')
if (keystorePropertiesFileBlink.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFileBlink))
}

def keystorePropertiesFileBlink1 = rootProject.file('key1.properties')
if (keystorePropertiesFileBlink1.exists()) {
    keystoreProperties1.load(new FileInputStream(keystorePropertiesFileBlink1))
}

def keystorePropertiesFileBlink2 = rootProject.file('key2.properties')
if (keystorePropertiesFileBlink2.exists()) {
    keystoreProperties2.load(new FileInputStream(keystorePropertiesFileBlink2))
}

def keystorePropertiesFile3 = rootProject.file('key3.properties')
if (keystorePropertiesFile3.exists()) {
    keystoreProperties3.load(new FileInputStream(keystorePropertiesFile3))
}


android {
    namespace "com.getaction.hello"
    compileSdkVersion 34/*flutter.compileSdkVersion*/
    ndkVersion flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId "com.getaction.hello"
        // You can update the following values to match your application needs.
        // For more information, see: https://docs.flutter.dev/deployment/android#reviewing-the-gradle-build-configuration.
        minSdkVersion 30
        targetSdkVersion 34/*flutter.targetSdkVersion*/
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
    }





    /*signingConfigs {
        releaseProject1 {
            storePassword keystoreProperties['storePassword']
            keyPassword keystoreProperties['keyPassword']
            keyAlias keystoreProperties['keyAlias']
            storeFile file(keystoreProperties['storeFile'])
        }
        releaseProject2 {
            storePassword keystoreProperties['storePassword']
            keyPassword keystoreProperties['keyPassword']
            keyAlias keystoreProperties['keyAlias']
            storeFile file(keystoreProperties['storeFile'])
        }
        releaseProject3 {
            storePassword keystoreProperties['storePassword']
            keyPassword keystoreProperties['keyPassword']
            keyAlias keystoreProperties['keyAlias']
            storeFile file(keystoreProperties['storeFile'])
        }
    }*/

    signingConfigs {

        releaseProject1 {
            storePassword keystoreProperties1['storePassword']
            keyPassword keystoreProperties1['keyPassword']
            keyAlias keystoreProperties1['keyAlias']
            storeFile file(keystoreProperties1['storeFile'])
        }

        releaseProject2 {
            storePassword keystoreProperties2['storePassword']
            keyPassword keystoreProperties2['keyPassword']
            keyAlias keystoreProperties2['keyAlias']
            storeFile file(keystoreProperties2['storeFile'])
        }

        releaseProject3 {
            storePassword keystoreProperties3['storePassword']
            keyPassword keystoreProperties3['keyPassword']
            keyAlias keystoreProperties3['keyAlias']
            storeFile file(keystoreProperties3['storeFile'])
        }
    }

    /*signingConfigs {
        releaseProject1 {
            storePassword keystoreProperties['storePassword1']
            keyPassword keystoreProperties['keyPassword1']
            keyAlias keystoreProperties['keyAlias1']
            storeFile file(keystoreProperties['storeFile1'])
        }
        releaseProject2 {
            storePassword keystoreProperties['storePassword2']
            keyPassword keystoreProperties['keyPassword2']
            keyAlias keystoreProperties['keyAlias2']
            storeFile file(keystoreProperties['storeFile2'])
        }
    }*/

    flavorDimensions "app"
    productFlavors {
        blink {
            dimension "app"
            applicationId "com.getaction.hello"
            versionCode 321
            versionName "2.1.36"
            signingConfig signingConfigs.releaseProject1
        }
        unity {
            dimension "app"
            applicationId "com.hallo.code_magic_test"
            versionCode 103
            versionName "1.0.21"
            signingConfig signingConfigs.releaseProject2
        }
        sportico {
            dimension "app"
            applicationId "com.getaction.sportico"
            versionCode 103
            versionName "1.0.21"
            signingConfig signingConfigs.releaseProject3
        }
    }

    buildTypes {
        releaseProject1 {
            shrinkResources false
            minifyEnabled false
        }
        releaseProject2 {
            shrinkResources false
            minifyEnabled false
        }
        releaseProject3 {
            shrinkResources false
            minifyEnabled false
        }
    }

   /* signingConfigs {
        release {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile keystoreProperties['storeFile'] ? file(keystoreProperties['storeFile']) : null
            storePassword keystoreProperties['storePassword']
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }*/

    /*buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig signingConfigs.debug
        }
    }*/
}

flutter {
    source '../..'
}

dependencies {
    implementation(platform("com.google.firebase:firebase-bom:32.6.0"))
    implementation("com.google.firebase:firebase-analytics")
}


//   flutter build apk --flavor blink2 -t lib/main.dart
//flutter run --flavor blink1
//   build/app/outputs/flutter-apk/app-blink2-release.apk
